require 'bundler/setup'

namespace :jently do
  desc "Start Jently."
  task :start do
    sh 'ruby jently_control.rb start'
  end

  desc "Stop Jently."
  task :stop do
    sh 'ruby jently_control.rb stop'
  end
end
